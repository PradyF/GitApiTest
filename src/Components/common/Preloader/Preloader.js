import React from 'react';
import preloader from '../../../assets/img/pre-loader.svg';

let Preloader = (props) => {
    return (<div>
        <img src={preloader} />
    </div>
    )
}

export default Preloader;